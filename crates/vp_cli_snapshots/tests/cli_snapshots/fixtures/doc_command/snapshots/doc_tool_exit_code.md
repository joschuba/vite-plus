# doc_tool_exit_code

A failing tool run keeps its own exit status; Vite+ adds nothing.

## `vp doc build`

**Exit code:** 3

```
Using provider `vitepress` (dependency marker `vitepress` in package.json)
fake vitepress: build failed
```
