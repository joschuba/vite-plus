# doc_default_package_redirect

The `defaultPackage` `doc` entry behaves as an implicit `-C` into the documentation package.

## `vp doc build`

```
note: vp doc: using packages/docs (defaultPackage in vite.config.ts)
Using provider `vitepress` (dependency marker `vitepress` in package.json)
fake vitepress argv: build
```
