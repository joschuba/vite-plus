# doc_detected_provider

A unique dependency marker selects the provider; the delegation line names the marker.

## `vp doc build`

```
Using provider `vitepress` (dependency marker `vitepress` in package.json)
fake vitepress argv: build
```
